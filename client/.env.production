# client/.env.production
# API calls go through Vercel's rewrite proxy (/api/* → Railway).
# This ensures session cookies (scoped to .vercentic.com) are forwarded
# correctly. Do NOT set VITE_API_URL — leave blank so apiClient uses
# relative /api paths which Vercel proxies to Railway.
VITE_API_URL=
